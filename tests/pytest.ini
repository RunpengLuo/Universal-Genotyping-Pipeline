[pytest]
# Run from the repo root as `pytest tests/`: pytest resolves this file from the
# args, so a bare `pytest` misses it and recurses into third_party/.
markers =
    network: hits an external host (deselected by default)
addopts = -m "not network"
